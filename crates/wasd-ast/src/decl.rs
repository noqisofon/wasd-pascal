//! 宣言（PROGRAM/UNIT/PROCEDURE/VAR/CONST/TYPEなど）のASTノード（プレースホルダ）。
//!
//! `UNIT`/`INTERFACE`/`IMPLEMENTATION`のようなUCSD拡張の宣言も、文法上は
//! dialectに関わらずパース可能にする（`Dialect`のドキュメント参照）。
//! ノード種別の設計はUCSD Pascal一次資料の確認後に行う。
